#include "sensors.h"

namespace sweethome
{
    
Sensors::Sensors(std::filesystem::path yaml_file)
{
    // Load(yaml_file);
    // m_name = GetSetting("Name");
    // m_unit = GetUnit(GetSetting("Unit"));
}

void Sensors::Update()
{
}

}